这是一个网盘的复刻项目，主要的模块包括两个部分，文件模块以及社交模块。项目主要使用C语言(用到了可变长度结构体)加上QT编写（使用到的组件有QTcpSocket、QListWidget、QDir、QFile、QTimer等）。整体架构上，封装了协议用来定义传输数据的格式，使用sqlite3来保存数据，连接相关的配置单独放在一个配置文件里。然后就是在一个opeWidget里放置了Friend*以及Book*指针，层次清晰。这个opeWidget在登陆成功之后打开。